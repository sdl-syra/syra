if ($('.moreConvsButton').length>0) {
	$('.moreConvsButton').remove();
	$('#conversations_container').append("<%= j render 'layouts/conversations' %>");
} else {
	$('#conversations_container').html("<%= j render 'layouts/conversations' %>");
}